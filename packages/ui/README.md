# packages/ui

Shared UI primitives ownership: Agent A

Use only when a component is truly reused across multiple frontend surfaces.  
Do not move fast-changing page-specific components here too early.

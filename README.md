# globe
A globe-based web platform that aggregates open MIT APIs, satellite and Earth observation data into a unified view. All datasets are mapped into an ontology layer, enabling AI-powered semantic exploration and conversational interaction with global data.
